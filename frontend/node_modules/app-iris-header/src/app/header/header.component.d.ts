import { OnInit, EventEmitter, ElementRef } from '@angular/core';
import { NgbModal } from '@ng-bootstrap/ng-bootstrap';
import { NoticeService } from '../core/services/notice.service';
import { OccurrenceService } from '../core/services/occurrence.service';
import { ProfileService } from '../core/services/profile.service';
import { Notice } from '../core/models/notice';
import { Occurrence } from '../core/models/occurrence';
import { Profile } from '../core/models/profile';
export declare class HeaderComponent implements OnInit {
    private noticeService;
    private occurrenceService;
    private profileService;
    private modalService;
    private elementRef;
    logOutName: any;
    nameUser: any;
    actionSelect: EventEmitter<{}>;
    notices: Notice[];
    occurrences: Occurrence[];
    profiles: Profile[];
    profile: string;
    unreadOccurrence: number;
    constructor(noticeService: NoticeService, occurrenceService: OccurrenceService, profileService: ProfileService, modalService: NgbModal, elementRef: ElementRef);
    ngOnInit(): void;
    openModal(content: any): void;
    readMessage(occurrence: Occurrence): void;
    changeProfile(): void;
    logOut(): void;
}
